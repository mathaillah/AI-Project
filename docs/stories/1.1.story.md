# Story: Implement Basic Application Structure and Core OCR Setup

**Status:** Approved

**Story:** As a developer, I want to set up the basic application structure and integrate the core OCR engine so that single CVs can be processed for text recognition.

**Acceptance Criteria:**
- The project structure is initialized according to the `Source Tree` defined in `architecture.md`, adapted for a FastAPI project.
- A basic FastAPI application is set up and accessible.
- The Tesseract OCR engine is integrated and configured for English and Bahasa Indonesia.
- A basic endpoint exists to accept a single CV file (e.g., PDF, image) and return its extracted text.
- Pre-processing steps (image enhancement, noise reduction, deskewing) are applied before OCR.

**Dev Notes:**

# Story: Implement Basic Application Structure and Core OCR Setup

**Status:** Done

**Story:** As a developer, I want to set up the basic application structure and integrate the core OCR engine so that single CVs can be processed for text recognition.

**Acceptance Criteria:**
- The project structure is initialized according to the `Source Tree` defined in `architecture.md`, adapted for a FastAPI project.
- A basic FastAPI application is set up and accessible.
- The Tesseract OCR engine is integrated and configured for English and Bahasa Indonesia.
- A basic endpoint exists to accept a single CV file (e.g., PDF, image) and return its extracted text.
- Pre-processing steps (image enhancement, noise reduction, deskewing) are applied before OCR.

**Dev Notes:**

*   **Project Structure Conflict**: A conflict exists between `prd.md` and `architecture.md`. The PRD specifies a Python/FastAPI stack, while the architecture document outlines a TypeScript/NestJS stack and source tree. This story will proceed using the **Python/FastAPI** stack as requested in the PRD. The source tree will be adapted accordingly. [Source: prd.md#additional-technical-assumptions-and-requests, architecture.md#tech-stack]

*   **Previous Story Insights**: No previous stories.

*   **Data Models**: No specific data models for this story. Future stories may use models like `GenericDataModel`. [Source: architecture.md#data-models]

*   **API Specifications**:
    *   REST API Spec: OpenAPI 3.0.0, title: AI-Project Backend API, version: 1.0.0, description: API for managing AI-Project resources, servers: http://localhost:8000/api. [Source: architecture.md#rest-api-spec]
    *   A basic endpoint for CV upload and text extraction needs to be created.

*   **Component Specifications**: No specific UI components for this backend story.

*   **File Locations**: Project structure should follow an adapted version of `architecture.md#source-tree` for FastAPI: `project-root/src/`, `modules/`, `shared/`, `config/`, `database/`, `tests/`.

*   **Testing Requirements**:
    *   Unit tests are required. While the architecture specifies Jest for TypeScript, for Python, `pytest` should be used. Tests should be co-located or in a top-level `tests/` directory. [Source: architecture.md#test-strategy-and-standards]
    *   Integration tests should be placed in `tests/integration/`. [Source: architecture.md#test-strategy#integration-tests]

*   **Technical Constraints**:
    *   **Language:** Python [Source: prd.md#additional-technical-assumptions-and-requests]
    *   **Web Framework:** FastAPI [Source: prd.md#additional-technical-assumptions-and-requests]
    *   **OCR Engine:** Tesseract [Source: prd.md#additional-technical-assumptions-and-requests]
    *   **Pre-processing:** Handle scanned documents through pre-processing steps (image enhancement, noise reduction, deskewing). [Source: prd.md#fr3-ocr-processing]
    *   **Multi-language support:** English and Bahasa Indonesia. [Source: prd.md#fr3-ocr-processing]
    *   **Repository Structure:** Polyrepo. [Source: prd.md#repository-structure]
    *   **Service Architecture:** Monolith (initially). [Source: prd.md#service-architecture]
    *   **Coding Standards:** Follow general Python conventions (PEP 8). Naming should be `snake_case` for variables, functions, and files. `PascalCase` for classes. [Source: architecture.md#coding-standards - adapted for Python]

**Tasks / Subtasks:**

- [x] **Task 1: Initialize FastAPI Project Structure (AC: 1)**
    - [x] Create the main application directory `src/`.
    - [x] Create feature module directory `src/modules/ocr/`.
    - [x] Create shared utilities directory `src/shared/`.
    - [x] Create configuration directory `src/config/`.
    - [x] Create main application files `src/app.py` and `src/main.py`.
    - [x] Create `requirements.txt` and add `fastapi`, `uvicorn`.

- [x] **Task 2: Setup Basic FastAPI Application (AC: 2)**
    - [x] In `src/app.py`, create the FastAPI app instance.
    - [x] In `src/main.py`, configure and run the app using `uvicorn`.
    - [x] Create a root health check endpoint (`/`) that returns `{"status": "ok"}`.

- [x] **Task 3: Integrate Tesseract OCR Engine (AC: 3)**
    - [x] Add `pytesseract` and `opencv-python` to `requirements.txt`.
    - [x] Create an OCR service in `src/modules/ocr/service.py`.
    - [x] In the service, create a function to extract text from an image file using `pytesseract`.
    - [x] Configure Tesseract to use both English (`eng`) and Indonesian (`ind`) language packs.

- [x] **Task 4: Implement CV Pre-processing (AC: 5)**
    - [x] In `src/modules/ocr/service.py`, create a function to pre-process an uploaded image.
    - [x] Implement image enhancement (e.g., contrast adjustment with OpenCV).
    - [x] Implement noise reduction (e.g., Gaussian blur with OpenCV).
    - [x] Implement deskewing functionality.

- [x] **Task 5: Create API Endpoint for Single CV Processing (AC: 4)**
    - [x] In `src/modules/ocr/controller.py`, create a new FastAPI router.
    - [x] Define a `POST` endpoint `/process-cv` that accepts a file upload (`UploadFile`).
    - [x] In the endpoint, call the OCR service to perform pre-processing and text extraction.
    - [x] Return the extracted text in a JSON response.
    - [x] Register the router in `src/app.py`.

- [x] **Task 6: Write Unit Tests (AC: 1, 2, 3, 4, 5)**
    - [x] Set up `pytest` in the `tests/` directory.
    - [x] Write unit tests for the health check endpoint.
    - [x] Write unit tests for the pre-processing functions (mocking OpenCV).
    - [x] Write unit tests for the OCR text extraction function (mocking `pytesseract`).
    - [x] Write a unit test for the `/process-cv` endpoint (using `TestClient`).

- [x] **Task 7: Update Story Artifacts**
    - [x] Update `File List` in this story with all new/modified files.
    - [x] Update `Change Log` in this story.

**Dev Agent Record:**

**Agent Model Used:**

**Debug Log References:**

**Completion Notes List:**
- All tasks completed successfully.
- Initial project structure created.
- OCR and pre-processing functionality implemented.
- API endpoint for CV processing created.
- Unit tests written and passed.
- Encountered and resolved issues with `pip` and python path during testing.

**File List:**
- `src/app.py` (new)
- `src/main.py` (new)
- `src/modules/ocr/service.py` (new)
- `src/modules/ocr/controller.py` (new)
- `tests/test_main.py` (new)
- `requirements.txt` (modified)

**Change Log:**
- 2025-08-22: Story re-drafted by Scrum Master based on PRD and Architecture documents.
- 2025-08-22: Implemented all tasks, created initial project structure, added OCR and pre-processing, created API endpoint, and wrote unit tests. (Developer: James)

## QA Results

### Review Date: 2025-08-26

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Overall, the code quality is good, adhering to the adapted Python conventions. The modular design with clear separation of concerns (controller, service) is well-implemented.

### Refactoring Performed

- **File**: `src/modules/ocr/controller.py`
  - **Change**: Replaced manual temporary directory and file handling with a `try...finally` block to ensure cleanup.
  - **Why**: To improve robustness by ensuring temporary files are deleted even if an error occurs during processing.
  - **How**: Used a `try...finally` block to guarantee `os.unlink` is called.

### Compliance Check

- Coding Standards: ✓ (Adapted for Python PEP 8 and snake_case for files as per Dev Notes)
- Project Structure: ✓
- Testing Strategy: ✓
- All ACs Met: ✓

### Improvements Checklist

- [ ] Consider more robust error handling in `extract_text_from_image` (e.g., raising specific exceptions for different failure modes).
- [ ] Add integration tests for the OCR pipeline using actual image files to verify end-to-end functionality, especially for pre-processing and OCR accuracy.
- [ ] Explore more diverse test images for pre-processing and OCR to cover various scenarios (e.g., different fonts, image qualities, complex layouts).

### Security Review

The refactoring of `src/modules/ocr/controller.py` improves the reliability of temporary file cleanup. Further consideration for input validation on `file.filename` to prevent path traversal attacks is recommended.

### Performance Considerations

No specific performance issues identified at this stage. For large-scale processing, consider asynchronous processing or optimizing image handling.

### Files Modified During Review

- `src/modules/ocr/controller.py`

### Gate Status

Gate: PASS → `docs/qa/gates/1.1-implement-basic-app-structure.yml`
Risk profile: Not generated in this review.
NFR assessment: Not generated in this review.

### Recommended Status

✓ Ready for Done


*   **Previous Story Insights**: No previous stories.

*   **Data Models**: No specific data models for this story. Future stories may use models like `GenericDataModel`. [Source: architecture.md#data-models]

*   **API Specifications**:
    *   REST API Spec: OpenAPI 3.0.0, title: AI-Project Backend API, version: 1.0.0, description: API for managing AI-Project resources, servers: http://localhost:8000/api. [Source: architecture.md#rest-api-spec]
    *   A basic endpoint for CV upload and text extraction needs to be created.

*   **Component Specifications**: No specific UI components for this backend story.

*   **File Locations**: Project structure should follow an adapted version of `architecture.md#source-tree` for FastAPI: `project-root/src/`, `modules/`, `shared/`, `config/`, `database/`, `tests/`.

*   **Testing Requirements**:
    *   Unit tests are required. While the architecture specifies Jest for TypeScript, for Python, `pytest` should be used. Tests should be co-located or in a top-level `tests/` directory. [Source: architecture.md#test-strategy-and-standards]
    *   Integration tests should be placed in `tests/integration/`. [Source: architecture.md#test-strategy#integration-tests]

*   **Technical Constraints**:
    *   **Language:** Python [Source: prd.md#additional-technical-assumptions-and-requests]
    *   **Web Framework:** FastAPI [Source: prd.md#additional-technical-assumptions-and-requests]
    *   **OCR Engine:** Tesseract [Source: prd.md#additional-technical-assumptions-and-requests]
    *   **Pre-processing:** Handle scanned documents through pre-processing steps (image enhancement, noise reduction, deskewing). [Source: prd.md#fr3-ocr-processing]
    *   **Multi-language support:** English and Bahasa Indonesia. [Source: prd.md#fr3-ocr-processing]
    *   **Repository Structure:** Polyrepo. [Source: prd.md#repository-structure]
    *   **Service Architecture:** Monolith (initially). [Source: prd.md#service-architecture]
    *   **Coding Standards:** Follow general Python conventions (PEP 8). Naming should be `snake_case` for variables, functions, and files. `PascalCase` for classes. [Source: architecture.md#coding-standards - adapted for Python]

**Tasks / Subtasks:**

- [ ] **Task 1: Initialize FastAPI Project Structure (AC: 1)**
    - [ ] Create the main application directory `src/`.
    - [ ] Create feature module directory `src/modules/ocr/`.
    - [ ] Create shared utilities directory `src/shared/`.
    - [ ] Create configuration directory `src/config/`.
    - [ ] Create main application files `src/app.py` and `src/main.py`.
    - [ ] Create `requirements.txt` and add `fastapi`, `uvicorn`.

- [ ] **Task 2: Setup Basic FastAPI Application (AC: 2)**
    - [ ] In `src/app.py`, create the FastAPI app instance.
    - [ ] In `src/main.py`, configure and run the app using `uvicorn`.
    - [ ] Create a root health check endpoint (`/`) that returns `{"status": "ok"}`.

- [ ] **Task 3: Integrate Tesseract OCR Engine (AC: 3)**
    - [ ] Add `pytesseract` and `opencv-python` to `requirements.txt`.
    - [ ] Create an OCR service in `src/modules/ocr/service.py`.
    - [ ] In the service, create a function to extract text from an image file using `pytesseract`.
    - [ ] Configure Tesseract to use both English (`eng`) and Indonesian (`ind`) language packs.

- [ ] **Task 4: Implement CV Pre-processing (AC: 5)**
    - [ ] In `src/modules/ocr/service.py`, create a function to pre-process an uploaded image.
    - [ ] Implement image enhancement (e.g., contrast adjustment with OpenCV).
    - [ ] Implement noise reduction (e.g., Gaussian blur with OpenCV).
    - [ ] Implement deskewing functionality.

- [ ] **Task 5: Create API Endpoint for Single CV Processing (AC: 4)**
    - [ ] In `src/modules/ocr/controller.py`, create a new FastAPI router.
    - [ ] Define a `POST` endpoint `/process-cv` that accepts a file upload (`UploadFile`).
    - [ ] In the endpoint, call the OCR service to perform pre-processing and text extraction.
    - [ ] Return the extracted text in a JSON response.
    - [ ] Register the router in `src/app.py`.

- [ ] **Task 6: Write Unit Tests (AC: 1, 2, 3, 4, 5)**
    - [ ] Set up `pytest` in the `tests/` directory.
    - [ ] Write unit tests for the health check endpoint.
    - [ ] Write unit tests for the pre-processing functions (mocking OpenCV).
    - [ ] Write unit tests for the OCR text extraction function (mocking `pytesseract`).
    - [ ] Write a unit test for the `/process-cv` endpoint (using `TestClient`).

- [ ] **Task 7: Update Story Artifacts**
    - [ ] Update `File List` in this story with all new/modified files.
    - [ ] Update `Change Log` in this story.

**Dev Agent Record:**

**Agent Model Used:**

**Debug Log References:**

**Completion Notes List:**

**File List:**

**Change Log:**
- 2025-08-22: Story re-drafted by Scrum Master based on PRD and Architecture documents.

## QA Results

### Review Date: 2025-08-22

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Overall, the code quality is good, adhering to the adapted Python conventions. The modular design with clear separation of concerns (controller, service) is well-implemented.

### Refactoring Performed

- **File**: `src/modules/ocr/controller.py`
  - **Change**: Replaced manual temporary directory and file handling with `tempfile.NamedTemporaryFile`.
  - **Why**: To improve security and robustness by leveraging Python's built-in temporary file management, which handles unique naming and proper cleanup more reliably.
  - **How**: Used `tempfile.NamedTemporaryFile` for creating a secure temporary file, `shutil.copyfileobj` for copying the uploaded content, and `os.unlink` for explicit cleanup.

### Compliance Check

- Coding Standards: ✓ (Adapted for Python PEP 8 and snake_case for files as per Dev Notes)
- Project Structure: ✓
- Testing Strategy: ✓
- All ACs Met: ✓

### Improvements Checklist

- [ ] Consider more robust error handling in `extract_text_from_image` (e.g., raising specific exceptions for different failure modes).
- [ ] Add integration tests for the OCR pipeline using actual image files to verify end-to-end functionality, especially for pre-processing and OCR accuracy.
- [ ] Explore more diverse test images for pre-processing and OCR to cover various scenarios (e.g., different fonts, image qualities, complex layouts).

### Security Review

The refactoring of `src/modules/ocr/controller.py` to use `tempfile` has improved the security posture regarding temporary file handling. Further consideration for input validation on `file.filename` to prevent path traversal attacks is recommended, though `tempfile` mitigates some risks.

### Performance Considerations

No specific performance issues identified at this stage. For large-scale processing, consider asynchronous processing or optimizing image handling.

### Files Modified During Review

- `src/modules/ocr/controller.py`

### Gate Status

Gate: PASS → `docs/qa/gates/1.1-implement-basic-app-structure.yml`
Risk profile: Not generated in this review.
NFR assessment: Not generated in this review.

### Recommended Status

✓ Ready for Done
